load("//bazel:api.bzl", "modular_cc_library")

# For linking the nvshmem hostlibs when compiling Mojo binaries from bazel
modular_cc_library(
    name = "hostlibs",
    visibility = ["//visibility:public"],
    deps = select({
        "//:linux_x86_64": ["@nvshmem_prebuilt//:hostlibs"],
        "//conditions:default": [],
    }),
)
